
% -type open_option() :: {depth, non_neg_integer()} | {scale, non_neg_integer()} | {chunk_size, non_neg_integer()} |
%                        {date, term()}.

% -type filter() :: average.
% -type reader_option() :: {filter, filter()} | {range, pulsedb:timestamp(), pulsedb:timestamp()}.


% -type iterator() :: term().

